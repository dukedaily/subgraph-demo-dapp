{
  "name": "Subgraph demo hardhat",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf *artifacts cache typechain-types",
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create dukedaily/subgraph-demo --node http://127.0.0.1:8020",
    "deploy-local": "graph deploy dukedaily/subgraph-demo --ipfs http://127.0.0.1:5001 --node http://127.0.0.1:8020"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "hardhat": "^2.11.1",
    "@graphprotocol/graph-cli": "^0.30.2",
    "@graphprotocol/graph-ts": "^0.27.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.7.3",
    "dotenv": "^16.0.2"
  }
}